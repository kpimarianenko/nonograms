import { buttonSize } from '@components/CircleButton/constants';

const headerVerticalPadding = 10;

export const headerHeight = buttonSize + headerVerticalPadding * 2;
export const headerHorizontalPadding = 10;
